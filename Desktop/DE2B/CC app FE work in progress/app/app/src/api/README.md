# API Layer (Frontend Only)

All backend communication must go through this directory.

- Use `client.ts` for all API calls.
- All functions are mocked for MVP.
- TODO comments show where to connect real backend endpoints.
- Update only this layer when integrating a real backend.
